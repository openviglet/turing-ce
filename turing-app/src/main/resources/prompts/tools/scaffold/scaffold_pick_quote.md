Pick the most representative quote for a term from a body of text. Pass the source `text`, the target `term`, and an optional `minLength` (characters, default 40). Returns the longest sentence of at least `minLength` characters that mentions the term, falling back to the longest sentence overall. Use when you need a pull quote and must extract it from supplied source text rather than fabricating one.
