Build a beat-structured markdown scaffold for narrative content. Provide a `title` and an optional comma-separated list of `beats` (section labels); returns a markdown skeleton with one `##` section per beat (defaulting to Hook / Development / Call to Action) for the assistant to fill in. Use when the user asks to draft an article, script, or newsletter and you want a consistent house structure before writing the prose.
