Rebuild a site RAG vector store from its currently-indexed documents. WRITE tool — requires the mcp:write scope and operator opt-in (turing.mcp-server.write-enabled). Parameter: site (from list_sites). Runs in the background and returns a task id; it requires a RAG-enabled agent configured on the site. Use it after bulk content changes so semantic search reflects the latest content.