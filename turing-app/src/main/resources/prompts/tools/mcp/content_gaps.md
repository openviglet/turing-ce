Surface content gaps for one site: high-demand, low-coverage queries — terms users search often but that return few results on average (below an internal threshold). Unlike top_failed_searches (exact zero-result), this highlights popular topics the index covers thinly, so the product/content team sees exactly what to write or improve next. Parameters: site (from list_sites) and rows (1-50, default 10).