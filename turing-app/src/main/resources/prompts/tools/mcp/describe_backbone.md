Describe this Turing server as a headless enterprise-search backbone — call this FIRST. Returns a JSON manifest with the product identity and version, which capabilities are live on this instance (search, cited rag_answer, headless agents, analytics, write/ingestion), and the full tool catalogue grouped by purpose (discovery, search, answer, agents, analytics, write) with a one-line description of each tool, plus a short getting-started sequence. Use it to self-onboard against Turing in a single round-trip instead of probing each tool: read the capability flags to learn what is available (e.g. whether rag_answer can compose a grounded answer or only return sources, and how many AI agents are configured), then follow the suggested order — list_sites → get_site_fields → search_site/rag_answer. Takes no parameters.
