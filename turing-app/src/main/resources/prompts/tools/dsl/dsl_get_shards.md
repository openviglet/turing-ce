Get shard information for all or a specific Semantic Navigation site (index). Returns details about each shard/core including: index name, shard/core name, primary/replica status, state, document count, store size, search engine type, node endpoint URL, and locale. Use this tool to monitor index health, check document counts per locale, and verify which search engine backend each site uses. If no index is specified, returns shards for all sites.