# Viglet Turing ES - Sample Configuration
# Copy this file and edit the values below.

# Server port
server.port=2700

# Database (default: embedded H2 — uncomment below for PostgreSQL)
# spring.datasource.url=jdbc:postgresql://localhost:5432/turing
# spring.datasource.username=turing
# spring.datasource.password=changeme
# spring.datasource.driver-class-name=org.postgresql.Driver
