name: Publish @viglet/turing-cli

# Publishes the Viglet Turing ES developer CLI to npm (E.7).
# Bump the version in frontend/packages/cli/package.json and commit it before
# dispatching this workflow. The build + tests run as a publish gate; npm itself
# rejects a version that is already published, so re-dispatching without a
# version bump is a safe no-op.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10.33.0

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 26.3.1
          registry-url: "https://registry.npmjs.org"
          scope: "@viglet"
          cache: "pnpm"
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install workspace deps
        working-directory: frontend
        run: pnpm install --frozen-lockfile=false

      - name: Build @viglet/turing-cli
        working-directory: frontend
        run: pnpm --filter "@viglet/turing-cli..." run build

      - name: Test (publish gate)
        working-directory: frontend/packages/cli
        run: pnpm run test

      - name: Publish to npm
        working-directory: frontend/packages/cli
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
