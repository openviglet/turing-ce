/**
 * Default-field document/group resolvers.
 *
 * <p>Moved to the framework-agnostic {@code @viglet/turing-sdk} package — this
 * module re-exports them so existing `../core/resolve` imports keep working.
 *
 * @since 2026.3.1
 */
export { resolveDocuments, resolveGroups } from "@viglet/turing-sdk";
