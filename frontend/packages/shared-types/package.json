{
  "name": "@viglet/turing-shared-types",
  "version": "2026.3.1",
  "private": true,
  "description": "Shared TypeScript types used by the Turing React SDK and the apps that consume it.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
