export type TurRestInfo = {
    username: string;
    password: string;
    firstName: string;
    lastName: string;
    admin: boolean;
    email: string;
    avatarUrl?: string;
    hasAvatar?: boolean;
}